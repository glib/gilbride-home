---
export interface Props {
  /**
   * The tag text content
   */
  tag: string;
  /**
   * Additional CSS classes to apply
   */
  class?: string;
}

const { tag, class: className = '' } = Astro.props;
---

<span class={`text-orange-400 text-xs font-medium ${className}`}>
  #{tag}
</span>